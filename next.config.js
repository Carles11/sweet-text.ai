/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   serverActions: true,
  //   nextScriptWorkers: true,
  // },
  reactStrictMode: true,
  distDir: 'out',
}

module.exports = nextConfig
